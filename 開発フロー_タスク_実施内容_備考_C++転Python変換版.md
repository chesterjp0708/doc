# 開発フロー タスク／実施内容／備考（C++バッチ → Python変換版）

> 設計 → 製造 → UT → IT の4工程について、左にタスク、中央に実施内容、右に備考（今回の実装でのタスクポイント・注意点）をまとめた表。
> リーダー提示の様式に合わせています。
>
> **今回の開発の前提**：複数のC++製サーバーサイドバッチをPythonへ変換する。各バッチはコード量は少ないものの共通依存があるため、**共通依存（依存部）を先にPython化 → その後、個別バッチを変換**する順序で進める。

---

## 設計段階

| タスク | 実施内容 | 備考（今回の実装でのポイント・注意点） |
|---|---|---|
| 要件・仕様の確認 | 要件定義書・既存資料をもとに仕様を読み込み、不明点を担当者と確認して仕様認識を確定する。 | 対象のC++バッチを一覧化し、各バッチの入力・出力・IFを棚卸しする。ここで**共通依存の切り分け**を行う。 |
| プログラム設計 | 基本設計に基づき、モジュール・クラス単位の粒度と処理ロジックを詳細設計書に落とし込む。 | **共通処理（依存部）と個別バッチ処理を分離して設計**する。C++実装をPythonへどう写像するかの論理設計を先に固める。共通依存は先に定義・設計する。 |
| 画面・帳票設計 | 画面構成・帳票レイアウトを設計書として作成し、レビューを通して確定する。 | バッチ処理は画面がないことが多い。出力ファイル・帳票ファイル・ログのみに絞って整理する。 |
| DB設計 | ER図・テーブル定義を作成し、項目・制約・インデックスを設計書に定める。 | 既存テーブル定義を確認し、**スキーマ変更は極力行わない**。C++のSQLを流用できるか、DB接続ライブラリの依存を確認する。 |
| 外部IF設計 | 他システムとの連携範囲・IF仕様を整理し、IF定義書を作成して連携先と合意する。 | サーバー系バッチは外部IF・他システム連携が多い。IF定義書とC++実装を突合せ、**ファイル入出力フォーマット（固定長・CSV・エンコーディング）**を正確に把握する。 |
| 状態遷移・エラー処理設計 | 状態遷移とエラー処理・例外処理の挙動を詳細設計書に定義する。 | C++のエラー処理（戻り値・errno・例外・**終了コード**）をPythonでどう表現するか定義する。バッチは異常終了時の挙動が重要。ログ出力の互換も決める。 |
| テスト計画書作成 | UT/ITのテスト観点・対象範囲を洗い出し、テスト計画書の骨子を作成する。 | **旧C++の実行結果とPythonの実行結果を突合せて同一性を確認**する回帰比較を計画に盛り込む。共通依存 → 個別バッチの順にテストする。 |
| 設計レビュー | 設計書の査読を実施し、指摘事項を設計書へ反映する。 | **C++とPythonの仕様差異・挙動一致の検証観点**でレビューする。共通依存の設計を先行してレビューする。 |

---

## 製造段階

| タスク | 実施内容 | 備考（今回の実装でのポイント・注意点） |
|---|---|---|
| 追加仕様の取り込み | 設計変更・追加仕様を確認し、製造物へ反映する。 | 変換中に判明したC++とPythonの仕様差を追加仕様として記録し、「挙動を変えない範囲」か「許容される差」かを明確にして進める。 |
| 環境構築 | リポジトリ作成・CI設定など、開発（製造）環境を整備する。 | **Pythonバージョン・依存ライブラリ・実行環境（ジョブスケジューラ・サーバー）**を整備。仮想環境/requirementsを用意し、**比較用に既存C++実行環境も保持**する。 |
| コーディング実装 | 詳細設計に基づき、画面・API・バッチなどを実装する。 | **共通依存を先に実装 → その後、個別バッチを変換**する。C++のデータ型・NULL/未初期化・整数幅・文字列エンコーディング・リソース（ファイル/DB接続）の扱いを確認する。グローバル変数・static・構造体の写像に注意。 |
| DB・データ作成 | マイグレーションを作成し、初期データ・マスタデータを投入する。 | 接続プール・トランザクション・SQL方言の互換を確認し、**C++とPythonでSQL実行結果が同一**になるよう実装する。マスタ・初期データは既存を流用する。 |
| 外部連携実装 | 他システムとの連携処理を実装し、必要に応じてモック・スタブを用意する。 | タイムアウト・リトライ・排他・**多重起動防止**を確認。ファイルパス・エンコーディングの互換を徹底する。 |
| コードレビュー対応 | コードレビューを実施し、指摘事項を修正・反映する。 | **共通依存のレビューを先行・重点化**する。型・エラー処理などC++とPythonの挙動差異をレビュー観点として共有する。 |
| セルフチェック | コーディング規約・静的解析・単体動作を自身で確認し、品質を担保する。 | **旧C++と同一入力で同一出力になるか**を確認。異常系も含めて、ログ・終了コードの互換を自己確認する。 |

---

## UT（単体テスト）段階

| タスク | 実施内容 | 備考（今回の実装でのポイント・注意点） |
|---|---|---|
| UT仕様書・ケース作成 | 正常系・異常系・境界値・防御的チェックを網羅したケースをUT仕様書として整備する。 | **既存C++のUT・テストデータを流用**する。共通依存のUTを先行し、境界値・NULL/空・エンコーディングを重点ケースにする。 |
| テストデータ準備 | UT実行に必要なテストデータを作成・投入する。 | 旧C++の実行に使ったデータを流用し、Pythonと**同一条件で比較可能**にしておく。 |
| UT実施 | 作成したテストケースを実行し、結果を記録する。 | 共通依存 → 個別バッチの順に実施。個別バッチは入力ファイル・引数・DBの組合せを確認する。 |
| カバレッジ計測 | カバレッジを計測し、未網羅のケースを洗い出して追加実行する。 | 変換で欠落しやすい分岐（エラー系・NULL系・空振り処理）をカバレッジで確認する。 |
| 不具合修正・再テスト | NGの場合は不具合を修正し、修正後にテストを再実行する。 | 不具合を「C++とPythonの仕様差起因」か「単純な変換誤り」かを切り分けて記録する。 |
| UT結果レポート作成 | 実施結果・問題点・カバレッジをまとめ、UT結果レポートを作成する。 | **旧C++との突合せ結果を報告し、挙動一致のエビデンス**として残す。 |

---

## IT（結合テスト）段階

| タスク | 実施内容 | 備考（今回の実装でのポイント・注意点） |
|---|---|---|
| 結合テスト計画・仕様書作成 | 結合テストの対象範囲・実施手順を定め、計画書・仕様書を作成する。 | サーバー系バッチの通し・連携を計画。**ジョブスケジューラ起動・他システム連携・複数バッチの連鎖**（後続バッチが前の出力を消費）を対象にする。 |
| 結合環境構築 | 結合環境を構築し、接続設定・連携先との疎通確認を行ってIT実施可能な状態にする。 | **C++とPythonを併走・切替できる環境**を構築し、本番相当のDB・ファイルシステム・サーバーを用意する。 |
| 結合テストデータ作成 | 連携先含むテストデータを用意し、データの組合せ・突合せ条件を満たすよう整備する。 | 本番データ・過去データを流用し、**C++とPythonで同一条件**になるよう突合せ条件を揃える。 |
| 結合テスト実施 | モジュール間の受け渡し・画面からDB・外部連携までの通し確認を行い、結果を記録する。 | **旧C++とPythonを同一入力で実行し、結果・ログ・出力ファイルを突合せ**る。連鎖バッチの整合も確認する。 |
| 不具合管理 | 検出した不具合をチケット起票し、優先度付けて修正する。 | 挙動差異に加えて**性能差（処理時間・メモリ使用量）**も記録し、劣化が見られないか確認する。 |
| 再テスト・回帰テスト | 修正後に再テスト・回帰テストを実施し、影響がないことを確認する。 | **共通依存を修正した場合の影響範囲が全バッチに及ぶ**ため、全体への波及確認を必ず行う。 |
| 結合テスト結果報告書作成 | 実施結果・残課題・完了判定の可否をまとめ、IT結果報告書として作成する。 | **並行稼働・切替リリースの判断材料**として、残課題（性能差・仕様差）を明記する。 |

---

*注：上記は一般的な場合の例。Web / 汎用システム / 既存システムなどプロジェクトの種類によってタスクの増減はある。*
