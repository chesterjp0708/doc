可以。这版以**代码实际业务流程为主线**，逐项关联原设计书：一致的地方引用设计内容，可能不同的地方标出依据和不确定性。函数同时展示代码名和日语业务名。

下面的日语 prompt 可直接使用：

```text
現行ソースコードを主軸とした、詳細な「業務フロー・実装分析報告書」を作成してください。既存のWord設計書と照合し、設計と一致する内容、および設計と異なる可能性がある内容を、報告書の該当箇所に明示してください。

■ 入力

・現行ソースコード：［フォルダーを指定］
・既存Word設計書：［ファイルまたはフォルダーを指定］
・対象機能：［対象機能を指定］
・出力先：［Markdownファイルのパスを指定］

■ 分析の基本方針

報告書は、コードから確認した実際の業務フローを主軸に構成してください。設計書の章立てに合わせることで、実装上の処理や分岐を省略しないでください。

現行コード、コード内コメント、既存設計書を区別して扱ってください。

・コード：実装動作を確認する根拠
・コメント：機能名や処理意図を理解する補助資料
・設計書：期待される仕様との照合資料

コメントと実装が矛盾する場合は、その矛盾を明記してください。
設計と実装に差異があっても、直ちに不具合や設計書の誤りと断定しないでください。

■ 調査方法

1. Word設計書の本文、表、図、注記を読み、照合に使用できるように章・節・項目を整理する。
2. 対象機能の入口を特定し、呼び出し先を追跡して実際の実行経路を調査する。
3. 業務動作に影響する共通関数、データ操作、設定値、外部連携も可能な範囲で確認する。
4. 関数の宣言・定義付近のコメント、関数内の処理コメント、関連する定義を確認する。
5. 正常系だけでなく、条件分岐、繰り返し、対象外、異常、停止、終了処理を確認する。

関数名やコメントだけで動作を判断しないでください。
動的な呼び出し、外部モジュール、設定不足などで追跡できない部分は、その境界と影響を明示してください。
コメントアウトされた旧処理は、現行の実行経路に含めないでください。

■ 関数の日本語名

各関数は、次の形式で表記してください。

日本語機能名（コード上の関数名）

日本語機能名は、以下の優先順位で決めてください。

1. 関数コメントに記載された日本語名称
2. 関連する機能コメントに記載された名称
3. 実装内容から作成した、処理の目的を表す日本語名称

名称の出典を「関数コメント」「関連コメント」「実装から付与」のいずれかで示してください。
実装から付与した名称は、正式名称と区別してください。
コメント中の名称が実際の処理と合わない場合は、名称を無条件に採用せず、相違を説明してください。

■ 報告書の構成

1. 対象と調査範囲

・対象機能、調査したソース、設計書の名称・版
・確認できる場合はソースのコミット識別子
・調査対象外、参照できなかった資料、分析上の制約

2. 業務の全体像

・何を契機に開始するか
・どの情報を受け取り、何を判断するか
・どの業務を、どの順序で実施するか
・どのデータや成果物を作成・更新するか
・どの条件で正常終了、異常終了、停止となるか

全体フローをMermaidで示してください。
概要図だけで済ませず、後続の詳細説明と対応付けてください。

3. 詳細な業務フローと関数別の実装

入口から実際の処理順に説明してください。
各業務段階に識別番号を付け、必要に応じて詳細フロー図を分割してください。

各段階では、以下を記載してください。

・業務上の目的
・担当する関数の日本語名とコード名
・日本語名の出典
・呼び出し元と呼び出される条件
・入力と、その業務上の意味
・処理内容を実行順に記載した説明
・条件分岐と各分岐の結果
・繰り返しの対象、継続条件、終了条件
・主要な呼び出し先と、その役割
・参照・更新するデータ、ファイル、状態
・出力、戻り値、および呼び出し元での扱い
・異常時の処理、後続処理への影響
・設計書との対応と判定
・根拠となるソースの位置

単なる関数一覧や呼び出し一覧にしないでください。
「この処理が業務上何を行い、その結果によって次に何が起こるか」が分かる説明にしてください。

重要な条件値、状態値、計算式、抽出条件、更新条件は具体的に記載し、業務上の意味を併記してください。
意味を確認できない値に、推測で業務名称を付けないでください。

共通関数の詳細説明は集約できますが、各呼び出し箇所で異なる入力条件や異常時の扱いは省略しないでください。

4. データと成果物の流れ

入力、参照情報、中間データ、更新情報、出力成果物について、次を整理してください。

・作成元または取得元
・使用する業務段階と関数
・使用目的
・更新内容
・引き渡し先
・設計書との対応

5. 異常・停止・再実行に関する実装

実装で確認できる範囲で、以下を整理してください。

・異常や停止の発生条件
・通知、ログ、状態更新
・中止される後続処理
・既に実施した更新や作成物の扱い
・再試行、再実行、復旧に関する処理

復旧手順やデータの巻き戻しを確認できない場合は、未確認としてください。

6. 設計書との照合一覧

詳細説明中の照合結果を、次の列で一覧化してください。

・照合番号
・業務段階
・関数の日本語名とコード名
・設計書の章・節・項目
・設計書の記載内容
・現行実装の内容
・判定
・差異または未確認の内容
・想定される業務影響
・追加確認事項

設計書に記載があるが、対応する実装を確認できなかった項目も含めてください。

7. 未確認事項と追加調査

何が未確認か、判断に必要な資料は何か、どの結論に影響するかを記載してください。

■ 設計書との照合方法

各業務段階の説明中に、以下の形式で照合結果を記載してください。

【設計照合：判定】
・設計書の位置：文書名、章・節・項目、表や図の番号
・設計書の内容：該当箇所の短い引用、または忠実な要約
・実装との対応：どの条件・処理・結果が対応するか
・差異／確認事項：相違点または判断できない点

引用と要約は明確に区別してください。
ページ番号が確認できない場合は作成せず、見出しや表の項目名で位置を示してください。

判定は、次のいずれかとしてください。

・一致
  確認した条件、処理、結果が設計書の記載と一致する。
  一部だけを確認した場合は、一致する範囲を明示する。

・差異を確認
  同じ条件・対象について、設計書と実装に具体的な相違がある。
  両方の根拠を示す。直ちに不具合とは断定しない。

・差異の可能性
  相違が疑われるが、設定、共通処理、運用条件などが未確認である。
  相違と考えた理由と、確定に必要な確認事項を示す。

・設計書に記載なし
  調査した設計書の範囲に、対応する記述が見つからない。
  不要な実装や仕様違反とは断定しない。

・対応実装を確認できず
  設計書に記載があるが、調査範囲では対応する実装を特定できない。
  未実装とは断定しない。

・照合不能
  設計書が読めない、参照先がないなどの理由で比較できない。

「一致」とする場合も、設計書の該当内容を必ず併記してください。
差異だけを列挙して、一致した処理の説明を省略しないでください。

■ 図と根拠の表記

・図の処理要素には、日本語機能名と主要な関数名を併記する。
・分岐には条件を記載する。
・並列処理、非同期処理、繰り返しを逐次処理と混同しない。
・大きな図は、全体図と業務段階ごとの詳細図に分ける。
・コード全文の転載を避け、必要な場合だけ短い抜粋を用いる。
・主要な説明には、ソースファイル、関数名、該当行などの確認可能な根拠を付ける。

■ 品質条件

・報告書はすべて日本語で作成する。
・詳細さを優先し、重要な分岐や下位処理を概要だけで省略しない。
・実装事実、コメントに記載された意図、設計書の仕様、分析上の推定を区別する。
・静的なコード調査だけで、実運用での動作確認済みとは記載しない。
・図、本文、対照表の処理順序や判定を一致させる。
・既存の分析報告があっても原始資料で再確認し、過去の修正履歴や重複説明を残さない。
・原本のWord設計書とソースコードは変更しない。

成果物を指定先に保存し、完了時にファイルへのリンク、確認した主要フロー、設計との差異または差異の可能性、残る未確認事項を簡潔に提示してください。
```



# AI活用度・優先度分析レポート（C++バッチ → Python変換）

> 本報告は「設計 → 製造 → UT → IT」の計画書に記載された各タスクについて、
> **AIでどこまで完結できるか（AI適性）** と **着手すべき優先度** を分析したもの。
>
> 前提：C++のバッチをAIでPythonへ変換する試行を行い、変換成功率は概ね良好だが、
> C++とPythonの言語仕様差（NULLと0、truthy/falsy、整数除算、文字コードなど）による
> **「人間が判断して修正すべき箇所」が残る**ことを確認済み。この前提を踏まえて分析。

---

## 凡例

### AI適性

| 記号 | 意味 |
|---|---|
| ◎ | AI主導で完結できる（人間は最終確認のみ） |
| ○ | AIが大きく支援できる（人間の判断・確認が必要） |
| △ | AIの寄与が小さい（人間主導・意思決定が必要） |

### 優先度

| 記号 | 意味 |
|---|---|
| A | 効果が大きく、AIの活用価値が高い（着手推奨） |
| B | AIで支援できるが、人間の判断が必須 |
| C | AIの寄与が小さく、人間主導が基本 |

優先度は「タスクの影響度 × AI適性」で判定。

---

## 設計段階

| タスク | AI適性 | 優先度 | AI活用の具体例 |
|---|---|---|---|
| 要件・仕様の確認 | ○ | A | 既存C++ソース・設計書からバッチ一覧・入出力・IFを棚卸しし、共通依存の切り分け案を生成。人間が確認・確定。 |
| プログラム設計 | ○ | A | C++実装をPythonへ写像する設計書の下書き生成。クラス/関数単位の粒度案や共通部/個別部の分離案を提示。人間が最終判断。 |
| 画面・帳票設計 | △ | C | バッチは画面がなく、出力ファイル・帳票・ログのみ。AIの寄与は小さく、人間が整理。 |
| DB設計 | ○ | B | 既存テーブル定義からER図・テーブル定義案を生成。SQL流用可否の分析を支援。 |
| 外部IF設計 | ○ | A | C++のIF実装・IF定義書を突合せ、ファイルフォーマット（固定長・CSV・エンコーディング）の仕様を抽出・整理。 |
| 状態遷移・エラー処理設計 | ○ | B | C++のエラー処理（戻り値・errno・例外・終了コード）を抽出し、Pythonでの表現案を生成。挙動の意味判断は人間。 |
| テスト計画書作成 | ○ | A | UT/ITの観点・対象範囲・回帰比較の方針を起草。テスト計画書の骨子を生成。 |
| 設計レビュー | △ | B | AIによる一次レビュー（仕様差異・欠落の指摘）は可能。最終判断は人間が実施。 |

---

## 製造段階

| タスク | AI適性 | 優先度 | AI活用の具体例 |
|---|---|---|---|
| 追加仕様の取り込み | ○ | B | C++とPythonの仕様差を差分抽出し、「挙動を変えない範囲」か「許容差」かを分類案として提示。人間が確定。 |
| 環境構築 | ○ | B | requirements.txt・Dockerfile・CI設定・仮想環境構成を生成。サーバー/スケジューラ設定は人間が調整。 |
| コーディング実装 | ◎ | A | **本件で最もAIの効果が高い**。共通依存→個別バッチの順にC++をPythonへ変換。出力は概ね動作するが、非互換箇所の修正は人間がレビュー。 |
| DB・データ作成 | ○ | B | マイグレーション・SQL・初期データ投入処理を生成。SQL方言や結果一致の確認は人間。 |
| 外部連携実装 | ○ | A | IF処理・モック/スタブを生成し、タイムアウト・リトライ・排他の実装を支援。 |
| コードレビュー対応 | ○ | A | AIによる一次レビュー（型・エラー処理など挙動差異の検出）を実施。是正の判断は人間。 |
| セルフチェック | ○ | A | 静的解析の実行と、旧C++との同一入力での出力・ログ・終了コード比較を自動化・支援。 |

---

## UT（単体テスト）段階

| タスク | AI適性 | 優先度 | AI活用の具体例 |
|---|---|---|---|
| UT仕様書・ケース作成 | ◎ | A | 境界値・NULL/空・エンコーディングなどを網羅したテストケースを自動生成。既存C++のUT・テストデータの流用案も提示。 |
| テストデータ準備 | ○ | B | 旧C++の実績データから、比較可能なテストデータを生成・整形。 |
| UT実施 | ○ | B | テスト自動実行（pytest等）のスクリプト生成・実行支援。実行結果の解釈は人間。 |
| カバレッジ計測 | ◎ | A | カバレッジ計測を自動実行し、未網羅の分岐（エラー系・NULL系・空振り）を抽出・指摘。 |
| 不具合修正・再テスト | ○ | A | 不具合を「仕様差起因」か「変換誤り」かに分類し、修正案を生成。仕様の確定は人間。 |
| UT結果レポート作成 | ○ | B | 実施結果・問題点・カバレッジをまとめた報告書を自動生成。 |

---

## IT（結合テスト）段階

| タスク | AI適性 | 優先度 | AI活用の具体例 |
|---|---|---|---|
| 結合テスト計画・仕様書作成 | ○ | B | 連鎖バッチ・スケジューラ起動・他システム連携を含むテスト計画書を起草。 |
| 結合環境構築 | △ | C | サーバー・DB・ファイルシステムなど実環境の構築は人間主導。AIは設定ファイル生成のみ補助。 |
| 結合テストデータ作成 | ○ | B | 本番・過去データから突合せ条件を満たすデータ生成を支援。 |
| 結合テスト実施 | ○ | B | 旧C++との同一入力による実行・突合せ（結果・ログ・出力ファイル）を自動化支援。最終判定は人間。 |
| 不具合管理 | ○ | B | 不具合の起票・分類・優先度の提案を支援。性能差の検出も支援。 |
| 再テスト・回帰テスト | ○ | A | 共通依存修正時の全バッチへの波及影響の回帰を自動実行。影響範囲の特定を支援。 |
| 結合テスト結果報告書作成 | ○ | B | 実施結果・残課題・完了判定をまとめた報告書を自動生成。 |

---

## 優先度サマリー（着手推奨順）

### A判別（効果大 × AI適性高・着手推奨）

| 順位 | 工程 | タスク |
|---|---|---|
| 1 | 製造 | コーディング実装 |
| 2 | UT | UT仕様書・ケース作成 |
| 3 | UT | カバレッジ計測 |
| 4 | 製造 | コードレビュー対応 |
| 5 | 製造 | セルフチェック |
| 6 | 設計 | 要件・仕様の確認 |
| 7 | 設計 | プログラム設計 |
| 8 | 設計 | 外部IF設計 |
| 9 | 設計 | テスト計画書作成 |
| 10 | 製造 | 外部連携実装 |
| 11 | UT | 不具合修正・再テスト |
| 12 | IT | 再テスト・回帰テスト |

### B判別（AI支援は有効、人間判断が必須）

- 環境構築、追加仕様の取り込み、DB設計、DB・データ作成、状態遷移・エラー処理設計
- テストデータ準備、UT実施、UT結果レポート作成
- 結合テスト計画・仕様書作成、結合テストデータ作成、結合テスト実施、不具合管理、結合テスト結果報告書作成
- 設計レビュー（AIは一次指摘のみ）

### C判別（人間主導・AIの寄与小）

- 画面・帳票設計（バッチに画面がほぼ無い）
- 結合環境構築（実環境の構築は人間）

---

## 分析からの提言

1. **コーディング実装・テストケース生成・カバレッジ計測** がAI活用の本丸。この3つを最優先に据えると工数削減効果が大きい。
2. **AIが変換した結果は鵜呑みにせず、人間レビューを必ず挟む**。C++とPythonの言語仕様差（NULLと0、truthy/falsy、整数除算、文字コード、static/未初期化、参照/ポインタ）に起因する非互換はAIが自動判断できないため。
3. **人間が担うべきは「仕様の確定」「許容差の判断」「最終レビュー」**。AIを「下書き・抽出・自動試験」に使い、意思決定は人間に残す分担が最適。
4. 特に共通依存→個別バッチの変換順は、AIの再現性・レビュー効率の観点でも有効。共通部を先に固めると個別変換のバラつきが減る。

---

*注：AI適性・優先度は一般的な傾向に基づく評価。実際の効果は対象バッチの規模・複雑さ・環境に依存する。*
